import { colors } from "./colors";
// import { fonts } from "./fonts";
export { Fonts } from "./Font";

const fonts = {
  body: "Lato",
  heading: "Ubuntu",
  monospace: "Roboto Mono",
};

export const theme = {
  colors,
  fonts,
};
